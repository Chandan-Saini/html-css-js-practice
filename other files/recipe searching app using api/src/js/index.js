
import search from "./models/Search";


import * as searchView from "./views/searchView"


console.log(`this is a sum ${searchView.sum(searchView.id, 2)} this is mul ${searchView.mul(3,2)} and this is string ${search}`);